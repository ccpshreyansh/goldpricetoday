// next-sitemap.ts

const config = {
    siteUrl: "https://goldpricetoday.vercel.app/", // Replace with your site's URL
    generateRobotsTxt: true,           // Optional: generates robots.txt
    changefreq: 'daily',               // Optional: frequency of updates
    priority: 0.7,                     // Optional: default priority
    sitemapSize: 7000,                 // Optional: max number of URLs per sitemap
  };
  
  export default config;
  